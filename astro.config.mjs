// @ts-check
import { defineConfig } from 'astro/config';

// TODO: replace with the real domain once purchased (PRD open question #1)
export default defineConfig({
  site: 'https://jaydenphu.dev',
});
